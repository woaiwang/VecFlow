# 文件路径：VecFlow/benchmarks/CMakeLists.txt

# Week3 引入 benchmark 时取消注释：
#
# add_executable(bench_distance bench_distance.cpp)
# target_link_libraries(bench_distance
#     PRIVATE
#         vecflow_core
#         benchmark::benchmark_main
# )